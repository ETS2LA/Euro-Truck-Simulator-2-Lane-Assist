<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:ETS2LA.UI.Views"
             xmlns:husk="https://github.com/d3ara1n/Huskui.Avalonia"
             x:Class="ETS2LA.UI.Views.ManagerView"
             x:DataType="views:ManagerView">
    <UserControl.Resources>
        <ItemsPanelTemplate x:Key="WrapPanelTemplate">
            <WrapPanel />
        </ItemsPanelTemplate>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
        <StackPanel>
            <ItemsControl x:Name="PluginList" ItemsSource="{Binding Plugins}" ItemsPanel="{StaticResource WrapPanelTemplate}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="views:PluginItem">
                        <husk:Card 
                            Padding="14"
                            Margin="10"
                            Width="332"
                            HorizontalAlignment="Left"
                            Tag="{Binding}"
                            Cursor="Hand"
                            Classes.Highlight="{Binding IsEnabled}"
                            PointerPressed="OnCardPointerPressed"
                        >
                            <StackPanel Spacing="6" Width="302">
                                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <husk:Tag IsVisible="{Binding IsEnabled}" Classes="Primary">
                                        <TextBlock Text="Enabled" FontSize="12" FontWeight="450" />
                                    </husk:Tag>
                                    <TextBlock Text="{Binding Name}" FontSize="14" />
                                </StackPanel>
                                <TextBlock Text="{Binding Description}" Classes="Description" FontSize="12" />
                            </StackPanel>
                        </husk:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border x:Name="PlaceholderPanel" IsVisible="False">
                <StackPanel Spacing="6">
                    <TextBlock Text="No plugins found" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text="Drop plugin DLLs into the Plugins folder (or build existing plugin projects) and hit Refresh." />
                    <Button Content="Refresh" Width="100" Click="OnRefreshButtonClick"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
