<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:ETS2LA.UI.Views.Settings"
             xmlns:husk="https://github.com/d3ara1n/Huskui.Avalonia"
             xmlns:ctxt="clr-namespace:ColorTextBlock.Avalonia;assembly=ColorTextBlock.Avalonia"
             xmlns:mdctl="clr-namespace:Markdown.Avalonia.Controls;assembly=Markdown.Avalonia"
             x:Class="ETS2LA.UI.Views.Settings.Updates"
             x:DataType="views:Updates">
    <Border Padding="20">
        <StackPanel Spacing="10">
            <StackPanel Spacing="10" MaxWidth="140" HorizontalAlignment="Left">
                <TextBlock Text="Update Channel:" />
                <ComboBox SelectedIndex="{Binding SelectedChannelIndex}">
                    <ComboBoxItem Content="release" />
                    <ComboBoxItem Content="nightly" />
                </ComboBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="24">
                <StackPanel VerticalAlignment="Center" Spacing="10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="You're currently on " />
                        <TextBlock Classes="Highlight" Text="{Binding CurrentVersion}"/>
                        <TextBlock Text="." />
                    </StackPanel>
                    <Button Content="Check for Updates" Width="150" HorizontalAlignment="Left" Click="OnCheckForUpdatesClick" AutomationProperties.Name="Check for Updates"/>
                </StackPanel>

                <TextBlock VerticalAlignment="Center" Text="→" FontSize="20" Classes="Description" IsVisible="{Binding IsUpdateAvailable}"/>

                <StackPanel VerticalAlignment="Center" IsVisible="{Binding IsUpdateAvailable}" Spacing="10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Latest Version: " />
                        <TextBlock Classes="Highlight" Text="{Binding LatestVersion}"/>
                    </StackPanel>
                    <Button Content="Install and Restart" Width="150" HorizontalAlignment="Left" Click="OnInstallAndRestartClick" AutomationProperties.Name="Install Update and Restart"/>
                </StackPanel>
            </StackPanel>

            <husk:MarkdownViewer
                Markdown="{Binding ReleaseNotes}"
                IsVisible="{Binding IsUpdateAvailable}"
                Margin="0 10 0 0"
                Background="Transparent"
                BorderThickness="0"
                Foreground="{StaticResource ControlForegroundBrush}"
                FontFamily="{StaticResource Geist}"
                FontSize="13"
            >
                <husk:MarkdownViewer.Styles>
                    <Style Selector="DockPanel.Markdown.List.Item">
                        <Setter Property="Margin" Value="2,2,0,2" />
                    </Style>
                </husk:MarkdownViewer.Styles>
            </husk:MarkdownViewer>
        </StackPanel>
    </Border>
</UserControl>