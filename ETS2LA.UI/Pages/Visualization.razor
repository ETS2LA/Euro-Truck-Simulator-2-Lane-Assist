@page "/visualization"

<div class="flex flex-col p-6">
    <h3>@(_("Sorry"))</h3>
    <p class="description">@(new HtmlString(_("This page hasn't been created yet, but it's coming <i>soon</i>.")))</p>
</div>