"""Uses the bettercam library to capture the screen and save it to the data variable at:
```
data["frameFull"] | Entire display image
data["frame"] | Cropped image based on the x,y,w,h values in the settings.
```"""